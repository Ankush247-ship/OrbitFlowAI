<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="80dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_marginEnd="12dp">

    <FrameLayout
        android:id="@+id/transitionIconBg"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:background="@drawable/bg_aspect_ratio_item">

        <ImageView
            android:id="@+id/transitionPreviewImage"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:visibility="gone" />
        
        <TextView
            android:id="@+id/transitionShortName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/str_aa"
            android:textColor="@color/textColor"
            android:textSize="18sp"
            android:fontFamily="sans-serif-medium" />

        <ImageView
            android:id="@+id/transitionFavStar"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="top|end"
            android:layout_margin="3dp"
            android:src="@drawable/ic_star_24"
            android:alpha="0.55"
            app:tint="@color/textColor" />
    </FrameLayout>

    <TextView
        android:id="@+id/transitionName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/str_none"
        android:textColor="@color/textColor"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />
</LinearLayout>
